app.py
print("Hello Jenkins")
Jenkinsfile
pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git credentialsId: 'github-ssh',
                    url: 'git@github.com:USERNAME/jenkins-demo.git'
            }
        }

        stage('Build') {
            steps {
                sh 'python3 app.py'
            }
        }
    }
}
